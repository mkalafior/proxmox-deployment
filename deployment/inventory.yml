---
# Dynamic inventory - will be populated during deployment
all:
  children:
    proxmox_vms:
      hosts:
        # VMs will be added here dynamically during deployment
    proxmox_servers:
      hosts:
        # Proxmox host will be added here if needed
