# Proxmox Connection Settings
PROXMOX_HOST=192.168.1.100
PROXMOX_USER=root@pam
PROXMOX_PASSWORD=your_proxmox_password
PROXMOX_NODE=proxmox

# Proxmox API Token Authentication (recommended for automation)
# TOKEN_ID=root@pam!deploy-token
# TOKEN_SECRET=your-token-secret

# VM Configuration (Optional - defaults provided)
VM_ID=200
VM_TEMPLATE=local:vztmpl/ubuntu-24.04-standard_24.04-2_amd64.tar.zst

# Cloudflare Settings (Optional - leave empty for internal-only deployment)
# CLOUDFLARE_DOMAIN=yourdomain.com
# APP_SUBDOMAIN=app

# Application Settings (Optional)
PORT=3000
NODE_ENV=production

# Service Hostname (Optional - for DNS resolution)
# SERVICE_HOSTNAME=my-api

# Container Tagging (Optional - for organization)
# PROJECT_TAG=my-project

# Example: Complete .env file for a web project
# PROJECT_TAG=web-services
# PROXMOX_HOST=192.168.1.100
# TOKEN_ID=root@pam!deploy-token
# TOKEN_SECRET=abc123-def456-ghi789
